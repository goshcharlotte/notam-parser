# Setup

1. Create a virtual environment if you haven't already. In the root of 'webapp':
```bash
python -m venv .webapp_env
```

2. Activate your environment and install the python libraries:
```bash
source .webapp_env/bin/activate
python -m pip install -r ./requirements.txt
```

3. run the webapp:
```bash
python app.py
```



